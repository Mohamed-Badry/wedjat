# ── SatNOGS Offline Pipeline ──
SATNOGS_API_TOKEN=your_token_here

# ── Space-Track (Historical TLE Fetching) ──
SPACETRACK_EMAIL=your_email@example.com
SPACETRACK_PASSWORD=your_password_here

# ── Docker Compose Services ──
POSTGRES_USER=wedjat
POSTGRES_PASSWORD=wedjat_pass
POSTGRES_DB=wedjat_db

# ── Backend API Config ──
DATABASE_URL=postgresql://wedjat:wedjat_pass@localhost:5432/wedjat_db

# Frontend: Only needed for local development (points to backend).
# In production/VPS, leave this blank as Caddy routes traffic natively.
PUBLIC_API_URL=http://localhost:8000

# ── Cloud VPS Infrastructure ──
MQTT_ALLOW_ANONYMOUS=false
MQTT_USERNAME=edge_station_1
MQTT_PASSWORD=your_secure_password
MQTT_USE_TLS=true

# ── Edge Agent (Ground Station) ──
# Use WSS and port 443 to connect securely through the VPS Caddy proxy
MQTT_BROKER_URL=vps.yourdomain.com
MQTT_BROKER_PORT=443
MQTT_USE_WSS=true
MQTT_USE_TLS=true
EDGE_LOCAL_BACKUP_PATH=./data/raw/fallback_buffer.csv
EDGE_RETRY_INTERVAL_SECONDS=5

COMPOSE_PROFILES=dev-cloud
